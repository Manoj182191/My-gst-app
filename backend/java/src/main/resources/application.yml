spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/helloca
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
server:
  port: 8080

jwt:
  secret: your-256-bit-secret
  expiration: 86400000 # 24 hours